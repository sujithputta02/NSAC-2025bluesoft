# Core API dependencies
fastapi>=0.104,<1
pydantic==2.5.0
python-multipart==0.0.6

# HTTP clients for API calls
requests>=2.31.0
aiohttp>=3.9.1
httpx>=0.27.0

# Essential data processing (lightweight)
numpy>=1.26,<3

# Remove heavy packages that exceed Vercel 250MB limit:
# pandas>=2.1,<3          # ~50MB
# xarray>=2023.11.0       # ~20MB  
# netCDF4>=1.6.5          # ~15MB
# scipy>=1.11,<2          # ~40MB
# matplotlib>=3.8,<4      # ~30MB
# seaborn>=0.13,<1        # ~20MB
# uvicorn>=0.24,<1        # Not needed for Vercel serverless